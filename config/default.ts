import { API_URL } from '@env';

export const PUBLIC_URL = `${API_URL}/public`;
export const DEFAULT_IMAGE = `${PUBLIC_URL}/default/placeholder.jpg`;

export const stackOptions = {
	headerShown: false,
	navigationBarHidden: true,
};
